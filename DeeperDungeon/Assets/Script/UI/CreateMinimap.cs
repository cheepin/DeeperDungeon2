using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class CreateMinimap : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//mapImage = GetComponent<Image>();
		//tex2 =new Texture2D(100,100);
		//for(int i = 0; i < 100; i++)
		//{
		//	for(int j = 0; j < 100; j++)
		//	{
		//		tex2.SetPixel(i,j,Color.red);
		//	}
		//}

		//sp = Sprite.Create(tex2, new Rect(10, 10, 100, 100), new Vector2(0, 0));
		//sp.name = "GeneratoedMap";
		//mapImage.sprite = sp;
		//mapImage.material.SetTexture(0,tex2);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
